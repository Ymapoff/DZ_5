image: Ubuntu

stack: jdk 11

branches:
  only:
    - main

build: off

install:
  - java -jar ./artifacts/app-card-delivery &


build_script:
  - chmod +x gradlew
  - ./gradlew test -Selenide.headless=true --info